import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', err => console.log('PAGE ERROR:', err.message)); await page.evaluateOnNewDocument(() => { window.addEventListener('error', e => console.log('CAUGHT ERR:', e.error?.stack || e.message)); window.addEventListener('unhandledrejection', e => console.log('CAUGHT REJ:', e.reason?.stack || e.reason)); }); await page.goto('http://localhost:5173/staff/login', {waitUntil: 'domcontentloaded'}); await new Promise(r => setTimeout(r, 2000)); await browser.close(); })();
