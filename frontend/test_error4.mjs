import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', err => console.log('CAUGHT ERR:', err.message)); await page.goto('http://localhost:5173/staff/login', {waitUntil: 'networkidle2'}); const rootHtml = await page.evaluate(() => document.getElementById('root').innerHTML); console.log('ROOT:', rootHtml.substring(0, 100)); await browser.close(); })();
