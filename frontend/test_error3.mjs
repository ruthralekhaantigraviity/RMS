import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); await page.evaluateOnNewDocument(() => { window.addEventListener('error', e => console.log('CAUGHT ERR:', e.message)); }); await page.goto('http://localhost:5173/staff/login', {waitUntil: 'domcontentloaded'}); await browser.close(); })();
